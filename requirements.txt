# Probe version for reproducible installs. The nightly workflow installs the probe
# from THIS pin (`pip install -r requirements.txt`, see observatory.yml), so bumping
# the version here is what adopts a new probe release into the nightly + the catalog
# regen. 0.40.0 adds `build-service-catalog` (the nightly now regenerates + signs the
# SERVICE catalog instead of serving the committed seed), plus the WS1 hard-evidence
# scoring floor, the WS2 plain-English verdict + flow, the monitor switch-grading fix
# (a switch is graded by the tokenizer shape, so CDN/wire noise no longer trips a false
# CONFIRMED drift), and the jurisdiction-to-PRC alert. Retains the catalog first-party
# provenance join the nightly catalog/registry regen depends on.
llm-provenance-probe==0.40.0
pyyaml>=6.0
# Live JSON API (api/). uvicorn to serve; httpx pulled in for TestClient.
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
